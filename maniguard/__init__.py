"""ManiGuard SFT configs for openpi (pi0.5 LoRA).

Namespace package holding :mod:`maniguard.openpi_sft`, which registers the
pi0.5 SFT ``TrainConfig``s into openpi's config registry at import time.
Deliberately minimal: importing it must not require anything beyond openpi
(no simulator, no version metadata).
"""
